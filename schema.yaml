title: mcp-rhel-manager placeholder schema
version: 0.1
description: |
  This is a minimal placeholder schema file created to satisfy
  repository references. Replace with the project's real schema
  as needed.
