### METADATA
{"file": "./scripts/hal-brain.py", "line": 624, "type": "print-statement"}

### SNIPPET
        else:
            print(f'  Skipped: {model}')

    print(f'\n  Removed {len(removed)} model(s).')
    return removed



### LLM_SUGGESTION
<START_SUGGESTION>
else:
    logging.info(f'  Skipped: {model}')

logging.info(f'\n  Removed {len(removed)} model(s).')
return removed
<END_SUGGESTION>

Rationale: Replaced print statements with logging calls to avoid potential issues in production environments and for better control over output levels.
