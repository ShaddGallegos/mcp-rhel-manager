### METADATA
{"file": "./scripts/hal-brain.py", "line": 620, "type": "print-statement"}

### SNIPPET
                else:
                    print(f'  ✗ Failed: {result.stderr.strip()}')
            except FileNotFoundError:
                print('  ollama command not found.')
        else:
            print(f'  Skipped: {model}')


### LLM_SUGGESTION
<START_SUGGESTION>
import logging

logging.error(f'  ✗ Failed: {result.stderr.strip()}')
except FileNotFoundError:
    logging.error('  ollama command not found.')
else:
    logging.info(f'  Skipped: {model}')
<END_SUGGESTION>

Rationale: Replace `print` statements with Python's built-in `logging` module for better control over log levels and output formatting.
