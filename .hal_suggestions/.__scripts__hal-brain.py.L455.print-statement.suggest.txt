### METADATA
{"file": "./scripts/hal-brain.py", "line": 455, "type": "print-statement"}

### SNIPPET

def task_plan(goal: str) -> list[dict]:
    """Decompose a complex goal into subtasks with optimal resource assignments."""
    print(f'\n🧠 HAL Task Planner: {goal}\n')
    print('Analyzing goal and decomposing into subtasks...\n')

    available = _get_available_models()

### LLM_SUGGESTION
<START_SUGGESTION>
    logging.info(f'\n🧠 HAL Task Planner: {goal}\n')
    logging.info('Analyzing goal and decomposing into subtasks...\n')
<END_SUGGESTION>

Rationale: Replace `print` statements with `logging.info` to adhere to best practices for logging in Python, improving the code's maintainability and flexibility.
