# Example environment file for mcp-code-fix
# Place a copy at /etc/default/mcp-code-fix (or set WATCH_BASES in systemd drop-in)
# WATCH_BASES is a whitespace-separated list of directories to scan.
# Example:
# WATCH_BASES="/home /opt /srv /home/sgallego/GIT"

WATCH_BASES="/home /opt /srv"
